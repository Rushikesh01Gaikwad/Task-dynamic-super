import { DirRoundDirective } from './dir-round.directive';

describe('DirRoundDirective', () => {
  it('should create an instance', () => {
    const directive = new DirRoundDirective();
    expect(directive).toBeTruthy();
  });
});
